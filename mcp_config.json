{
  "tavily": {
    "command": "npx",
    "args": ["-y", "tavily-mcp"],
    "transport": "stdio",
    "env": {"TAVILY_API_KEY": "${TAVILY_API_KEY}"}
  },
  "fetch": {
    "command": "uvx",
    "args": ["mcp-server-fetch"],
    "transport": "stdio"
  }
}
